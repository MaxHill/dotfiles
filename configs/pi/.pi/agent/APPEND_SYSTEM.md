When you need user clarification, a decision, or additional input before proceeding, use the `ask_user` tool (from `@eko24ive/pi-ask`) instead of guessing.

Do not ask clarification questions as plain assistant text when `ask_user` can be used. Use one focused question per `ask_user` call. If you accidentally asked in plain text, immediately re-ask via `ask_user`.

When reporting information to me, be extremely concise and sacrifice grammar for sake of concision.
