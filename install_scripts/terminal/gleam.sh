# cd /tmp
# GLEAM_VERSION="1.5.0" # Use known good version
# NAME=gleam-v${GLEAM_VERSION}-aarch64-apple-darwin
# wget -q "https://github.com/gleam-lang/gleam/releases/download/v${GLEAM_VERSION}/${NAME}.tar.gz"
#
# tar -xzf "${NAME}.tar.gz"
# sudo mv ./gleam /usr/local/bin/
#
# rm "${NAME}.tar.gz"
# cd -
