windows:
  - name: candidates
    root: ~/code/candidates
    layout: tiled
    panes:
      - cd api && docker-compose up
      - commands:
          - cd api && clear
      - cd web && y start
      - commands:
          - kp 8080 && cd web && clear
        focus: true
